# keystore.properties — TEMPLATE ONLY
# Copy this file to android/keystore.properties and fill in real values.
# This template file is safe to commit. The real keystore.properties is gitignored.

storeFile=../shaadi-mantrana-release.jks
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=shaadi-mantrana
keyPassword=YOUR_KEY_PASSWORD
